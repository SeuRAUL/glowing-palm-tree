<div class="card">
  <h2 class="card-header">Editing Appointment</h2>
  <div class="card-body">
    <%= render 'form', appointment: @appointment %>
  </div>
</div>

<br/>
<%= link_to 'Show', @appointment, class:"btn btn-secondary btn-sm" %>
<%= link_to 'Back', appointments_path, class:"btn btn-dark btn-sm" %>
